name: airflow-ect
channels:
  - conda-forge
dependencies:
  # Python
  - python >=3.10
  # Required
  - airflow < 3.0
  - cartopy
  - cftime
  - dask
  - esa-climate-toolbox
  - geopandas
  - matplotlib-base
  - numcodecs
  - numpy
  - pandas
  - pip
  - pyproj
  - python-dateutil
  - shapely
  - six
  - xarray
  - xcube >= 1.8.0
  - xcube-cci >= 0.11.7
  - zarr <3  # until we can ensure zarr 3 compatibility
  - pip
  - pip:
      - apache-airflow-providers-docker
      - apache-airflow-providers-cncf-kubernetes < 10.4
      - dag-factory
  # Test libs
  - pytest
  - pytest-cov
